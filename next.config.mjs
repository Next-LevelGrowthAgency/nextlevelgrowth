/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    // Add real remote image domains here once production imagery/CDN is chosen.
    remotePatterns: [],
  },
};

export default nextConfig;
